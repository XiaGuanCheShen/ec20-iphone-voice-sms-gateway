; Public IPv6 transports for a SIP client. Keep Issabel generated IPv4 transports intact.
[transport-udp6]
type=transport
protocol=udp
bind=[::]:5160

[transport-tcp6]
type=transport
protocol=tcp
bind=[::]:5160

